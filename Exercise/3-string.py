#Write a Python function that takes a string and two indices, start and end, and returns a new string that consists of every second character between the start and end indices (inclusive). For example, for the input string "abcdefghijklmnopqrstuvwxyz" and indices 4 and 20, the output should be "egikmoq". Handle cases where the start or end indices are out of

